# This file has been auto-generated by i3-config-wizard(1).
# It will not be overwritten, so edit it as you like.
#
# Should you change your keyboard layout somewhen, delete
# this file and re-run i3-config-wizard(1).
#

# i3 config file (v4)
#
# Please see http://i3wm.org/docs/userguide.html for a complete reference!

set $mod Mod4 

# Font for window titles. Will also be used by the bar unless a different font
# is used in the bar {} block below. ISO 10646 = Unicode
font -misc-fixed-medium-r-normal--13-120-75-75-C-70-iso10646-1
# The font above is very space-efficient, that is, it looks good, sharp and
# clear in small sizes. However, if you need a lot of unicode glyphs or
# right-to-left text rendering, you should instead use pango for rendering and
# chose a FreeType font, such as:
# font pango:DejaVu Sans Mono 10
# Use Yosemite SanFransisco Font
#font pango:System San Francisco Display 14

# Use Ubuntu font
font pango: Ubuntu Medium 14

# Window borders
for_window [class="^.*"] border pixel 2
# Make Hangouts float
for_window [class="crx_nckgahadagoaajjgafhacjanaoiihapd"] floating enable
# Make KeePass2 float
for_window [class="KeePass2"] floating enable
# Compton for opacity etc
exec_always compton --config /home/dizak/.config/compton.conf
# xfce4 useful services
# power manager allowing brightness Fn control
#exec_always xfce4-power-manager
# volume manager allowing sound Fn control
#exec xfce4-volumed

# Fn keybindigs without any Graphical Environment elements
# increse volume
#bindsym XF86AudioRaiseVolume exec "amixer sset Master 5%+"
# decrease volume
#bindsym XF86AudioLowerVolume exec "amixer sset Master 5%-"
# mute
#bindsym XF86AudioMute exec "amixer sset Master 0%"
# increase monitor brightness
#bindsym XF86MonBrightnessUp exec xbacklight +5
# decrease monitor brightness
#bindsym XF86MonBrightnessDown exec xbacklight -5


# Use Mouse+$mod to drag floating windows to their wanted position
floating_modifier $mod

# start a terminal
bindsym $mod+Return exec urxvt 

# kill focused window
bindsym $mod+Shift+q kill

# start rofi (a program launcher)
#bindsym $mod+d exec rofi -combi-modi window#drun#ssh -font "San Francisco Display 18" -show combi -show-icons -theme dmenu
bindsym $mod+d exec rofi -combi-modi window#drun#ssh -show combi -show-icons -theme dmenu
# There also is the (new) i3-dmenu-desktop which only displays applications
# shipping a .desktop file. It is a wrapper around dmenu, so you need that
# installed.
# bindsym $mod+d exec --no-startup-id i3-dmenu-desktop

# change focus
# This is changed since $mod+l is wanted for i3lock
#bindsym $mod+j focus left
#bindsym $mod+k focus down
#bindsym $mod+l focus up
#bindsym $mod+semicolon focus right

# alternatively, you can use the cursor keys:
bindsym $mod+Left focus left
bindsym $mod+Down focus down
bindsym $mod+Up focus up
bindsym $mod+Right focus right

# move focused window
bindsym $mod+Shift+j move left
bindsym $mod+Shift+k move down
bindsym $mod+Shift+l move up
bindsym $mod+Shift+semicolon move right

# alternatively, you can use the cursor keys:
bindsym $mod+Shift+Left move left
bindsym $mod+Shift+Down move down
bindsym $mod+Shift+Up move up
bindsym $mod+Shift+Right move right

# split in horizontal orientation
bindsym $mod+h split h

# split in vertical orientation
bindsym $mod+v split v

# enter fullscreen mode for the focused container
bindsym $mod+f fullscreen

# change container layout (stacked, tabbed, toggle split)
bindsym $mod+s layout stacking
bindsym $mod+w layout tabbed
bindsym $mod+e layout toggle split

# toggle tiling / floating
bindsym $mod+Shift+space floating toggle

# change focus between tiling / floating windows
bindsym $mod+space focus mode_toggle

# focus the parent container
bindsym $mod+a focus parent

# focus the child container
#bindsym $mod+d focus child

# define workspaces
set $chrome_workspace "0: "
set $terminals_workspace "1: "
set $mail_workspace "2: "
set $development_workspace_1 "3: "
set $development_workspace_2 "4: "
set $development_workspace_3 "5: "
set $development_workspace_4 "6: "
set $development_workspace_5 "7: "
set $development_workspace_6 "8: "
set $private_workspace "9: "

# switch to workspace
bindsym $mod+0 workspace $chrome_workspace
bindsym $mod+1 workspace $terminals_workspace
bindsym $mod+2 workspace $mail_workspace
bindsym $mod+3 workspace $development_workspace_1
bindsym $mod+4 workspace $development_workspace_2
bindsym $mod+5 workspace $development_workspace_3
bindsym $mod+6 workspace $development_workspace_4
bindsym $mod+7 workspace $development_workspace_5
bindsym $mod+8 workspace $development_workspace_6
bindsym $mod+9 workspace $private_workspace

# move focused container to workspace
bindsym $mod+Shift+0 move container to workspace $chrome_workspace
bindsym $mod+Shift+1 move container to workspace $terminals_workspace
bindsym $mod+Shift+2 move container to workspace $mail_workspace
bindsym $mod+Shift+3 move container to workspace $development_workspace_1
bindsym $mod+Shift+4 move container to workspace $development_workspace_2
bindsym $mod+Shift+5 move container to workspace $development_workspace_3
bindsym $mod+Shift+6 move container to workspace $development_workspace_4
bindsym $mod+Shift+7 move container to workspace $development_workspace_5
bindsym $mod+Shift+8 move container to workspace $development_workspace_6
bindsym $mod+Shift+9 move container to workspace $private_workspace

# move window to scratchpad
bindsym $mod+Shift+minus move scratchpad
# show the scratchpad window
bindsym $mod+minus scratchpad show

# duplicate view from display to HDMI output
bindsym $mod+Shift+d exec xrandr --output HDMI-1 --same-as eDP-1

# reload the configuration file
bindsym $mod+Shift+c reload
# restart i3 inplace (preserves your layout/session, can be used to upgrade i3)
bindsym $mod+Shift+r restart
# lock screen
#bindsym $mod+l exec "i3lock -c '#000000'"
# exit i3 (logs you out of your X session)
#bindsym $mod+Shift+e exec "i3-nagbar -t warning -m 'You pressed the exit shortcut. Do you really want to exit i3? This will end your X session.' -b 'Yes, exit i3' 'i3-msg exit'"
# autolock
exec xautolock -time 5 -noclose -corners 000- -cornersize 100 -locker "i3lock --color '#111111' --show-failed-attempts"
# power off
bindsym $mod+Control+Delete exec /home/dizak/.config/i3blocks-contrib/shutdown_menu/shutdown_menu

# resize window (you can also use the mouse for that)
mode "resize" {
        # These bindings trigger as soon as you enter the resize mode

        # Pressing left will shrink the windowâs width.
        # Pressing right will grow the windowâs width.
        # Pressing up will shrink the windowâs height.
        # Pressing down will grow the windowâs height.
        bindsym j resize shrink width 10 px or 10 ppt
        bindsym k resize grow height 10 px or 10 ppt
        bindsym l resize shrink height 10 px or 10 ppt
        bindsym semicolon resize grow width 10 px or 10 ppt

        # same bindings, but for the arrow keys
        bindsym Left resize shrink width 10 px or 10 ppt
        bindsym Down resize grow height 10 px or 10 ppt
        bindsym Up resize shrink height 10 px or 10 ppt
        bindsym Right resize grow width 10 px or 10 ppt

        # back to normal: Enter or Escape
        bindsym Return mode "default"
        bindsym Escape mode "default"
}

bindsym $mod+r mode "resize"

# Floating windows maximum size
floating_maximum_size 800 x 800

# Start i3bar to display a workspace bar (plus the system information i3status
# finds out, if available)
bar {
        status_command i3blocks -c ~/.config/i3/i3blocks.conf
        tray_output primary
        #mode hide
}
# set wallpaper
exec_always feh --bg-scale /home/dizak/.wallpapers/jets.jpg
# set conky system info and usage display
exec_always conky --config /home/dizak/.config/.conkyrc
